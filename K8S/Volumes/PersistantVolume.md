define les volume et comment les connecte a des connection , nfs, on host ...
o peut avoir du dynamoc provisioning (les ressources sont creer automatiquement )..
et les static provisioning (ressource creer dynamiquement ).

# Persistan Volume 
retaition policy : 
- Delete : supprimer les elements quand le persistant vol est supprimer
- retain : garde les elemetns
- recycle : garde les connection (style mount) mais supprime les data.

